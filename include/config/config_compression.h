#ifndef CONFIG_COMPRESSION_H
#define CONFIG_COMPRESSION_H

/* Size of libdeflates's buffer in kibibytes (yes kib not kb). HackerSM64's implementation uses a full Mb however, we use 32 kib*/
#define DEFLATE_BUFFER 32

#endif